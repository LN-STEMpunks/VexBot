sudo: true

language: java

pre-install:
  - sudo apt install python3 python3-pip
  - pip3 install pyfrc
  - pip3 install pynetworktables

install:
  - pushd Java
  - ./gradlew build
  - popd
  - pushd Python
  - python3 src/robot.py test

jdk: oraclejdk8
